node_modules
dist
.DS_Store
*.log
.vite
.env
.env.*
!.env.example
coverage
.tsbuildinfo
*.tsbuildinfo
.playwright-mcp/

# MkDocs
site/
.venv/

# Generated docs/llms.txt + docs/llms-full.txt (npm run docs:llms) are committed
# on purpose so the published site serves them even without a regen step.
scripts/.geodata-cache/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Screenshots from the visual-verification step. Kept out of git: a full-page
# capture runs into the megabytes and nothing in the build reads them.
/*.png
!docs/**/*.png

# Claude Code: the agent definitions are shared with the team; worktrees and
# any other local session state are not.
.claude/*
!.claude/agents/
