node_modules
dist
coverage
.changeset
*.drawio
package-lock.json
template

# MkDocs content: prettier flattens `!!!` admonition bodies (4-space indent),
# breaking Material rendering. The docs site owns its own markdown formatting.
docs
